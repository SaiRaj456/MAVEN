ssh -i "new.pem" ubuntu@ec2-44-222-177-164.compute-1.amazonaws.com

sudo apt update

sudo apt-get install docker.io


sudo apt install git

Sudo apt install nano

git clone  https://github.com/bharadwaj360/Lms.git

cd Lms

sudo docker login

sudo docker build -t lms-app .

sudo docker run -d --name -p 8980:8080 lms-app
sudo docker run -d --name lms-container -p 8980:8080 lms-app


nginx

curl.exe -LO "https://storage.googleapis.com/kubernetes-release/release/v1.30.0/bin/windows/amd64/kubectl.exe"
minikube start

kubectl create deployment nginx --image=nginx

kubectl get deployments

kubectl get pods

kubectl expose deployment nginx --port=80 --type=NodePort

kubectl get svc

minikube service nginx

minikube service nginx --url

nagios

docker run --name nagiosdemo -p 8888:80 jasonrivers/nagios:latest

docker pull jasonrivers/nagios:latest

compose

services:
  wordpressapp:
    image: wordpress:latest
    ports:
      - "9080:8080"
    depends_on:
      - db

  db:
    image: mysql:8.0
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: mydb


git config --global user.name "your name"

git config --global user.email "your email"



docker build -t sairaj121/docker .

docker images

docker run -d -p 8098:8080 tomcat

docker pull tomcat

docker run -d -p 6086:8080 tomcat

docker ps

docker login

docker-compose.yml 

docker-compose up -d 

docker-compose down 

docker ps

docker-compose up -d

docker-compose down


